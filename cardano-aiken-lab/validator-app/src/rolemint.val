module rolemint

type Role {
  Admin
  Minter
  Viewer
}

validator {
  fn main(datum: Role, _redeemer: (), _ctx: ScriptContext) -> Bool {
    case datum {
      Role::Admin  -> true,
      Role::Minter -> true,
      _            -> false
    }
  }
}
